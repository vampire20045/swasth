import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

const useStyles import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';
import { makeStyles } from '@mui/styles';

// Custom styles for the component
const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '10px 20px',
    backgroundColor: '#00CCDD',
    color: 'black',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  textButton: {
    color: '#FFF',
  },
  outlinedButton: {
    borderColor: '#FFF',
    color: '#FFF',
    '&:hover': {
      backgroundColor: '#005A69',
      borderColor: '#005A69',
      color: '#FFF',
    },
  },
});

function Land() {
  const classes = useStyles();

  return (
    <div>
      <div className={classes.container}>
        <Button variant="text" className={classes.textButton}>
          Disaster
        </Button>

        <div className={classes.buttonGroup}>
          <Button variant="outlined" className={classes.outlinedButton}>
            How to prepare
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Emergency
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Past experience
          </Button>
import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';
import { makeStyles } from '@mui/styles';

// Custom styles for the component
const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '10px 20px',
    backgroundColor: '#00CCDD',
    color: 'black',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  textButton: {
    color: '#FFF',
  },
  outlinedButton: {
    borderColor: '#FFF',
    color: '#FFF',
    '&:hover': {
      backgroundColor: '#005A69',
      borderColor: '#005A69',
      color: '#FFF',
    },
  },
});

function Land() {
  const classes = useStyles();

  return (
    <div>
      <div className={classes.container}>
        <Button variant="text" className={classes.textButton}>
          Disaster
        </Button>

        <div className={classes.buttonGroup}>
          <Button variant="outlined" className={classes.outlinedButton}>
            How to prepare
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Emergency
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Past experience
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            SignUp
          </Button>
        </div>
      </div>
    </div>
  );
}

export default Land;
import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';
import { makeStyles } from '@mui/styles';

// Custom styles for the component
const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '10px 20px',
    backgroundColor: '#00CCDD',
    color: 'black',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  textButton: {
    color: '#FFF',
  },
  outlinedButton: {
    borderColor: '#FFF',
    color: '#FFF',
    '&:hover': {
      backgroundColor: '#005A69',
      borderColor: '#005A69',
      color: '#FFF',
    },
  },
});

function Land() {
  const classes = useStyles();

  return (
    <div>
      <div className={classes.container}>
        <Button variant="text" className={classes.textButton}>
          Disaster
        </Button>

        <div className={classes.buttonGroup}>
          <Button variant="outlined" className={classes.outlinedButton}>
            How to prepare
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Emergency
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Past experience
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            SignUp
          </Button>
        </div>
      </div>
    </div>
  );
}

export default Land;
import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';
import { makeStyles } from '@mui/styles';

// Custom styles for the component
const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '10px 20px',
    backgroundColor: '#00CCDD',
    color: 'black',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  textButton: {
    color: '#FFF',
  },
  outlinedButton: {
    borderColor: '#FFF',
    color: '#FFF',
    '&:hover': {
      backgroundColor: '#005A69',
      borderColor: '#005A69',
      color: '#FFF',
    },
  },
});

function Land() {
  const classes = useStyles();

  return (
    <div>
      <div className={classes.container}>
        <Button variant="text" className={classes.textButton}>
          Disaster
        </Button>

        <div className={classes.buttonGroup}>
          <Button variant="outlined" className={classes.outlinedButton}>
            How to prepare
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Emergency
import { Link } from 'react-router-dom';
import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';
import { makeStyles } from '@mui/styles';

// Custom styles for the component
const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '10px 20px',
    backgroundColor: '#00CCDD',
    color: 'black',
  },
  buttonGroup: {
    display: 'flex',
    gap: '10px',
  },
  textButton: {
    color: '#FFF',
  },
  outlinedButton: {
    borderColor: '#FFF',
    color: '#FFF',
    '&:hover': {
      backgroundColor: '#005A69',
      borderColor: '#005A69',
      color: '#FFF',
    },
  },
});

function Land() {
  const classes = useStyles();

  return (
    <div>
      <div className={classes.container}>
        <Button variant="text" className={classes.textButton}>
          Disaster
        </Button>

        <div className={classes.buttonGroup}>
          <Button variant="outlined" className={classes.outlinedButton}>
            How to prepare
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Emergency
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            Past experience
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            SignUp
          </Button>
        </div>
      </div>
    </div>
  );
}

export default Land;
d" className={classes.outlinedButton}>
            Past experience
          </Button>
          <Button variant="outlined" className={classes.outlinedButton}>
            SignUp
          </Button>
        </div>
      </div>
    </div>
  );
}

export default Land;
